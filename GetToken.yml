info:
  name: GetToken
  type: http
  seq: 1

http:
  method: POST
  url: http://192.168.5.30:8071/api/Authen/GetToken
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
