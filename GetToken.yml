info:
  name: GetToken
  type: http
  seq: 1

http:
  method: POST
  url: http://appportal.sabina.co.th:8071/api/Authen/GetToken
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
