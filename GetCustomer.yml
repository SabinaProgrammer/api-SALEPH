info:
  name: GetCustomer
  type: http
  seq: 18

http:
  method: POST
  url: http://appportal.sabina.co.th:8071/api/Customer/GetCustomer
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
