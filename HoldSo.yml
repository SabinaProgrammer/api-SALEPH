info:
  name: HoldSo
  type: http
  seq: 20

http:
  method: POST
  url: http://192.168.5.30:8071/api/Customer/HoldSo
  body:
    type: json
    data: |-
      {
        "customerid": "0001",
        "enddate": "2025-09-09",
        "username": "CHAT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
